# Dependencias (se reinstalan con npm install)
node_modules/
backend/node_modules/

# Variables de entorno / secretos — NUNCA subir esto a GitHub
.env
backend/.env

# Logs
npm-debug.log*
*.log

# Sistema
.DS_Store
Thumbs.db
