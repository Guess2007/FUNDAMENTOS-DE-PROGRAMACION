# Copia este archivo a ".env" y ajusta los valores según tu instalación de XAMPP

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=genshin_calc
DB_PORT=3306

# Cambia esto por una cadena larga y aleatoria antes de usar en producción
JWT_SECRET=cambia_esta_clave_por_una_segura

PORT=3000
